{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npm run build",
  "installCommand": "npm install",
  "regions": ["fra1"],
  "functions": {
    "app/api/**": {
      "maxDuration": 30
    }
  }
}
